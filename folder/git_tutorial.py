def add_numbers(a,b,c):
    """Return the sum of a, b, and c."""
    return sum([a, b, c])

# 1. extend add_numbers to allow arbitrary number of parameters
# 2. investigate and resolve security exploit of add_numbers
# 3. make the implementation of add_numbers more performant
# 4. add documentation for add_numbers()

def calc_difference(a, b):
    return a - b